<?xml version="1.0" encoding="utf-8"?>
<!-- Nền tròn mờ cho nút icon: nổi rõ trên nền camera mà không che nhiều. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@color/btn_bg" />
    <stroke
        android:width="1dp"
        android:color="@color/btn_stroke" />
</shape>
